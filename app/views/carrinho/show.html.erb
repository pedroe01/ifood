<%= turbo_frame_tag "carrinho" do %>
<header class="bg-danger py-1 mb-5">
  <div class="container px-4 px-lg-5 my-5 d-flex justify-content-center">
    <h1 class="display-3 fw-bolder text-center mx-2">Seu Carrinho</h1>
    <i class="bi bi-cart display-3 mx-2"></i>
  </div>
</header>
  <% if @carrinho.itenspedido.count > 0 %>
    <%= render "carrinho/itenspedido" %>
    <h2 class="text-center">Total: <%= @carrinho.total %></h2>
  <% else %>
    <p>Seu carrinho está vazio</p>
  <% end %>
<% end %>
